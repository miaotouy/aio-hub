# =================================================================
# AIO Hub 开发环境配置模板
# 复制此文件为 .env.local 进行本地个性化配置
# =================================================================

# --- 端口配置 ---
# 前端 Vite 开发服务器端口 (默认: 1420)
AIO_PORT=1420
# Vite 热更新 (HMR) 端口 (默认: 1421)
AIO_HMR_PORT=1421
# Rust 后端 LLM 代理服务端口 (默认: 16655)
AIO_PROXY_PORT=16655

# --- 实例与隔离 ---
# 实例标识后缀 (用于多分支并行开发)
# 1. 若为空: 使用全局数据目录 (%APPDATA%)，适合日常开发
# 2. 若不为空 (如 dev-a):
#    - 应用标识变为 com.mty.aiohub.dev-a
#    - 数据目录隔离至 ./.dev-data/dev-a
#    - 端口会自动基于此后缀进行偏移，防止冲突
AIO_ID_SUFFIX=

# --- UI 展示 ---
# 产品名称前缀，方便在任务栏区分不同实例
AIO_PRODUCT_NAME=AIO Hub (Dev)