import{_ as o,o as t,c as d,a3 as a}from"./chunks/framework.BTFg6ESq.js";const m=JSON.parse('{"title":"应用启动性能优化调查报告","description":"","frontmatter":{},"headers":[],"relativePath":"Plan/app-startup-optimization-investigation.md","filePath":"Plan/app-startup-optimization-investigation.md","lastUpdated":1781399933000}'),i={name:"Plan/app-startup-optimization-investigation.md"};function c(s,e,n,r,l,p){return t(),d("div",null,[...e[0]||(e[0]=[a(`<h1 id="应用启动性能优化调查报告" tabindex="-1">应用启动性能优化调查报告 <a class="header-anchor" href="#应用启动性能优化调查报告" aria-label="Permalink to &quot;应用启动性能优化调查报告&quot;">​</a></h1><blockquote><p>日期:2026-06-12 范围:桌面端主窗口启动全链路(Rust setup → WebView → JS 解析 → Vue mount → <code>initMainApp</code> → MainLayout 首帧) 依据:源码走查 + <code>dist/</code>(2026-06-11 构建,v0.6.3-alpha.6)产物量化 + AppData 日志中 4 次真实启动的分步耗时 性质:调查报告,不含代码改动</p></blockquote><h2 id="实施记录" tabindex="-1">实施记录 <a class="header-anchor" href="#实施记录" aria-label="Permalink to &quot;实施记录&quot;">​</a></h2><h3 id="_2026-06-12-第一轮实施" tabindex="-1">2026-06-12 第一轮实施 <a class="header-anchor" href="#_2026-06-12-第一轮实施" aria-label="Permalink to &quot;2026-06-12 第一轮实施&quot;">​</a></h3><p>已开始落地阶段一的低风险启动优化,当前实际改动如下:</p><ul><li><code>monacoShikiSetup.ts</code> 去除顶层 <code>stream-monaco</code> 静态导入,改为 <code>initMonacoShikiThemes()</code> 内动态导入;<code>initMainApp</code>/<code>initDetachedApp</code> 不再在首帧前等待 Monaco/Shiki 主题预热,改为 ready 后后台任务。</li><li><code>preset-icons.ts</code> 中 Lobe 图标从 <code>?raw</code> 改为 <code>?url</code>,避免 834 个 SVG raw 字符串内联进启动 chunk;同步调整 <code>useThemeAwareIcon</code> 按 URL 拉取并处理 SVG。</li><li><code>vite.config.ts</code> 的 <code>vendor-vue</code> 分包规则从 <code>id.includes(&quot;vue&quot;)</code> 改为精确匹配 Vue/Pinia 包名,避免 Shiki 语法、<code>@vue-flow</code>、<code>vuedraggable</code> 等懒资源被误并入启动必载 vendor。</li><li><code>appInitStore.ts</code> 主窗口首帧前只等待设置、日志配置、主题和工具/插件注册;模型元数据、用户档案、启动项、分离窗口管理、同步总线和编辑器主题初始化均改为 ready 后后台任务。</li><li><code>auto-register.ts</code> 的剩余工具注册从串行 <code>for-await</code> 改为 <code>Promise.allSettled</code> 并发加载,保留失败模块收集和插件加载/排序/ready 的收尾流程。</li></ul><p>与原计划/后续计划的差异:</p><ul><li>P0-2 在本文后续已标注升级为 <code>stream-monaco-removal-plan.md</code> 的整体移除方案。本轮没有执行完整移除,只先实施阶段一的&quot;动态 import + 不阻塞首帧&quot;方案;完整删除 <code>stream-monaco</code>、<code>shiki</code>、MonacoSourceViewer 和设置项仍以单独计划为准。</li><li>P2-8 的 AMD Monaco <code>editor.main.nls.js</code> 404 / defer 处理尚未落地,需继续确认 <code>@tomjs/vite-plugin-monaco-editor</code> 可用配置或改为后续统一 ESM Monaco 时一并处理。</li></ul><p>验证结果:</p><ul><li><code>bun run build:tsc</code> 通过。</li><li><code>bun run build:vite</code> 通过。构建后 <code>dist/index.html</code> 直连资源约 <strong>10.97 MiB / 190 个文件</strong>,较调查基线 <strong>17.5 MiB / 145 个文件</strong> 下降约 <strong>6.5 MiB</strong>。</li><li>直连资源 Top 中已不再出现 <code>editor.api2-*.js</code>;旧 <code>vendor-vue</code> 的 3.95 MiB 误并问题回落到约 <strong>112 KiB</strong>。</li><li>当前最大残留为 <code>main</code> 约 <strong>4.0 MiB</strong>、<code>vendor-editor</code> 约 <strong>1.38 MiB</strong>、<code>docxParser</code> 约 <strong>802 KiB</strong>、<code>AudioPlayer</code> 约 <strong>302 KiB</strong>。构建日志继续报告 <code>src/services/plugin-ui.ts</code> 对工具 registry 的静态导入导致 <code>auto-register.ts</code> 动态导入无效,说明阶段二 P0-1 仍是下一步最大收益点。</li></ul><h3 id="_2026-06-12-第二轮实施" tabindex="-1">2026-06-12 第二轮实施 <a class="header-anchor" href="#_2026-06-12-第二轮实施" aria-label="Permalink to &quot;2026-06-12 第二轮实施&quot;">​</a></h3><p>继续落地阶段二 P0-1 和阶段一 P2-8 的低风险部分,当前实际改动如下:</p><ul><li><code>plugin-ui.ts</code> 去除通用组件和工具 registry 的 <code>{ eager: true }</code>;<code>components</code> map 仍同步导出,但值改为 <code>defineAsyncComponent</code> 包装的异步组件。</li><li>通用组件名继续从 <code>src/components/common/*.vue</code> 文件名推导;工具组件名继续从 <code>src/tools/{toolId}/{toolId}.registry.ts</code> 的目录名推导,首次渲染对应插件组件时再动态导入 registry 并读取 <code>toolConfig.component</code>。</li><li><code>vite.config.ts</code> 新增 <code>monaco-remove-missing-nls-script</code> post HTML transform,移除 <code>@tomjs/vite-plugin-monaco-editor</code> 注入的 <code>editor.main.nls.js</code> 脚本行,消除产物中的固定 404。</li></ul><p>与原计划/后续计划的差异:</p><ul><li>P0-1 原设想是&quot;异步读取 registry 后按 <code>toolConfig.component</code> 包装&quot;;实际实现为了保持 <code>window.AiohubUI.components</code> 的同步可枚举契约,组件名仍必须从路径推导,只把 registry 模块内容延迟到组件首次渲染时读取。</li><li>P2-8 本轮只移除了缺失的 nls 脚本。<code>@tomjs/vite-plugin-monaco-editor</code> 本地文档未提供 <code>defer</code>/脚本属性配置项;直接改 defer 会改变 AMD Monaco 与入口 module 的执行时序,暂不纳入本轮。</li><li>构建产物仍会将大量动态 import 写入 <code>index.html</code> 的 <code>modulepreload</code>;因此 P0-1 的首轮收益主要体现为入口 chunk 执行体积下降和 registry 不再由 <code>plugin-ui</code> 同步求值,并未把 HTML 直连资源总量继续压到阶段二目标。后续若要继续压直连请求,需要单独评估 Vite/Rolldown 的 modulepreload 策略或拆出更晚才可达的插件 UI 目录入口。</li></ul><p>验证结果:</p><ul><li><code>bun run build:tsc</code> 通过。</li><li><code>bun run build:vite</code> 通过。</li><li>构建后 <code>dist/index.html</code> 不再包含 <code>editor.main.nls.js</code>;仍保留 <code>loader.js</code> 与 <code>editor.main.js</code>。</li><li>构建后 <code>main</code> chunk 约 <strong>606 KiB</strong>,较第一轮记录的约 <strong>4.0 MiB</strong> 明显下降;<code>dist/index.html</code> 直连资源约 <strong>10.5 MiB / 215 个文件</strong>。</li><li>构建日志中已不再出现 <code>src/services/plugin-ui.ts</code> 对工具 registry 的静态导入导致 <code>auto-register.ts</code> 动态导入无效的提示;仍有若干 <code>INEFFECTIVE_DYNAMIC_IMPORT</code> 提示来自其他组件/工具的真实静态引用,属于后续拆分范围。</li></ul><h3 id="_2026-06-12-第三轮实施" tabindex="-1">2026-06-12 第三轮实施 <a class="header-anchor" href="#_2026-06-12-第三轮实施" aria-label="Permalink to &quot;2026-06-12 第三轮实施&quot;">​</a></h3><p>继续落地 P0-2 的升级形态,执行 <a href="./stream-monaco-removal-plan">stream-monaco-removal-plan.md</a>,当前实际改动如下:</p><ul><li>删除 <code>monacoShikiSetup.ts</code> 与消息代码块的 <code>MonacoSourceViewer.vue</code>;</li><li><code>CodeBlockNode.vue</code> 固定使用 CodeMirror,移除 <code>codeEditorEngine</code> 设置、类型、默认值、prop/context、llm-chat 透传和测试器引擎选择器;</li><li><code>appInitStore.ts</code> 删除首帧后后台 Monaco/Shiki 主题初始化任务;</li><li><code>package.json</code> 删除 <code>stream-monaco</code>、<code>shiki</code> 两个直接依赖,<code>bun install</code> 已刷新 <code>bun.lock</code>;</li><li>CodeMirror 语言映射补齐 <code>c</code>、<code>toml</code>、<code>lua</code>、<code>powershell</code>,并额外加入 <code>diff</code>、<code>ini/properties</code>;</li><li>同步更新 rich-text-renderer/llm-chat 架构文档、用户渲染设置文档和 rich-text-renderer 性能调查文档。</li></ul><p>与原计划/后续计划的差异:</p><ul><li>P0-2 在第一轮已先改为动态 import + 后台任务,所以本轮实际从 <code>appInitStore.ts</code> 删除的是后台 <code>initMonacoShikiThemes()</code> 调度,而不是旧调查正文中描述的首帧前 await。</li><li><code>bun.lock</code> 仍包含 VitePress 间接依赖的 <code>shiki</code>,这不代表应用路径仍直接依赖 Shiki;项目直接依赖和 <code>stream-monaco</code> 子树已移除。</li><li>额外清理了 rich-text-renderer 测试器 store 的 <code>codeEditorEngine</code> 存量配置字段,避免新配置继续写入死字段。</li></ul><h3 id="_2026-06-14-第四轮实施" tabindex="-1">2026-06-14 第四轮实施 <a class="header-anchor" href="#_2026-06-14-第四轮实施" aria-label="Permalink to &quot;2026-06-14 第四轮实施&quot;">​</a></h3><p>继续落地阶段二 P2-11 中低风险的资源下沉与异步化,当前实际改动如下:</p><ul><li><code>App.vue</code> 将 <code>MainLayout</code> 改为 <code>defineAsyncComponent</code> 动态导入,让入口只保留根初始化、全局 provider 与 LoadingScreen。</li><li><code>GlobalProviders.vue</code> 将全局图片/视频/音频预览器、转写弹窗、模型选择弹窗和媒体生成信息弹窗改为异步组件;视频、音频和模型选择弹窗增加 <code>v-if</code> 可见性门控,避免全局 provider 挂载时立即加载对应重组件。</li><li><code>main.ts</code> 移除全局 <code>viewerjs/dist/viewer.css</code> 与 <code>katex/dist/katex.min.css</code> 导入;<code>ImageViewer.vue</code> 与 <code>KatexRenderer.vue</code> 改为在各自功能路径内引入样式。</li></ul><p>与原计划/后续计划的差异:</p><ul><li>本轮没有处理 Element Plus 全量导入和插件全局 shim 延后(P1-7),该项仍需结合插件契约单独回归。</li><li>本轮没有处理 @tomjs AMD Monaco 的长期 ESM 统一(P2-8),也没有调整 Rust 侧窗口显示时机或 AssetCatalog 后台化。</li><li><code>GlobalProviders</code> 仍保留 <code>SyncServiceProvider</code>、<code>NotificationCenter</code> 的静态加载;这些是更贴近全局常驻能力的组件,暂不在本轮拆分。</li></ul><p>验证结果:</p><ul><li><code>bun run build:tsc</code> 通过。</li><li><code>bun run build:vite</code> 通过;构建后 <code>dist/index.html</code> 仍不包含 <code>editor.main.nls.js</code>。</li><li>构建后 <code>dist/index.html</code> 直连资源约 <strong>7.67 MiB / 177 个文件</strong>。当前最大残留仍包括 <code>main</code> 约 <strong>1.47 MiB</strong>、<code>vendor-element</code> 约 <strong>1.04 MiB</strong>、<code>vendor-editor</code> 约 <strong>1.41 MiB</strong>、AMD <code>editor.main</code> 约 <strong>3.68 MiB</strong> 与大型懒加载工具/worker chunk。</li><li>构建日志仍有若干 <code>INEFFECTIVE_DYNAMIC_IMPORT</code> 提示,主要来自 <code>NotificationCenter</code>/<code>DocumentViewer</code> 等真实静态引用与若干工具内部交叉引用,属于后续拆分项。</li></ul><h2 id="结论先行" tabindex="-1">结论先行 <a class="header-anchor" href="#结论先行" aria-label="Permalink to &quot;结论先行&quot;">​</a></h2><p>当前启动耗时由两段构成,<strong>两段的主导因素都已定位</strong>:</p><ol><li><p><strong>空窗期(进程启动 → 窗口可见)</strong>:主窗口以 <code>visible(false)</code> 创建,直到 Vue <code>app.mount()</code> 完成才 <code>show()</code>(<code>src/main.ts:180-191</code>)。而 mount 之前必须下载并解析 <strong>17.5 MiB / 145 个文件</strong> 的 JS+CSS(<code>dist/index.html</code> 实测,入口 + 128 个 modulepreload + 15 个 CSS)。这段时间用户看不到任何窗口,<code>index.html</code> 里精心做的内联 loading 动画在主窗口流程中<strong>永远不可见</strong>。</p></li><li><p><strong>加载期(窗口可见 → MainLayout 首帧)</strong>:<code>appInitStore.initMainApp()</code> 是 10 步纯串行瀑布,实测 <strong>1.22–1.48s</strong>(4 次启动),其中约 0.9–1.1s 来自三类本不必阻塞首帧的工作:全量工具注册(414–527ms)、用户启动项任务(377–481ms)、Monaco/Shiki 语法预热(135–190ms)。</p><p>17.5 MiB 静态载荷的归因(按大小排序),<strong>前四项合计约 12 MB,全部是&quot;本应懒加载却被静态链入入口&quot;的资源</strong>:</p></li></ol><table tabindex="0"><thead><tr><th>占比</th><th>chunk</th><th>大小</th><th>根因</th></tr></thead><tbody><tr><td>22%</td><td><code>vendor-vue</code></td><td>3.95 MB</td><td><code>manualChunks</code> 用 <code>id.includes(&quot;vue&quot;)</code> 匹配(<code>vite.config.ts:196</code>),把 <strong>Shiki 的 vue/vue-html/vue-vine 语法文件、@vue-flow、sortablejs</strong> 等仅被懒加载代码使用的包,强行并入入口必载的 vendor chunk</td></tr><tr><td>20%</td><td><code>editor.api2</code></td><td>3.63 MB</td><td>ESM 版完整 Monaco。<code>appInitStore.ts:13</code> → <code>monacoShikiSetup.ts:14</code> 顶层静态 <code>import { registerMonacoThemes } from &quot;stream-monaco&quot;</code>,而 stream-monaco 顶层 <code>import * as monaco from &quot;monaco-editor&quot;</code></td></tr><tr><td>13%</td><td><code>model-metadata</code></td><td>2.34 MB</td><td><code>preset-icons.ts:13</code> 用 <code>{ eager: true, query: &quot;?raw&quot; }</code> 内联 <strong>834 个 lobehub SVG 全集</strong></td></tr><tr><td>10%</td><td><code>main</code>(入口)</td><td>1.89 MB</td><td><code>main.ts</code> 全量导入 Element Plus、全部 EP 图标、plugin-sdk/plugin-ui 并挂到 <code>window</code>;<code>MainLayout</code> 被 <code>App.vue:9</code> 静态导入</td></tr><tr><td>7%</td><td><code>vendor-editor</code></td><td>1.23 MB</td><td>codemirror + @guolao/vue-monaco-editor,经 common 组件(RichCodeEditor/DocumentViewer)被 <strong>plugin-ui 的 eager glob</strong> 拖入</td></tr><tr><td>8%</td><td><code>vendor-element</code>+CSS</td><td>1.16 MB + 355 KB</td><td><code>main.ts:7,94</code> 全量 <code>app.use(ElementPlus)</code>(为插件 shim 契约服务)</td></tr><tr><td>~12%</td><td>docxParser 802KB、macro-engine 329KB、AudioPlayer 301KB、katex 257KB、markdown-it、jszip、llmChatStore、各工具 registry…</td><td>~2.2 MB</td><td><strong><code>plugin-ui.ts:24</code> 对全部 38 个工具 registry 做 <code>import.meta.glob(..., { eager: true })</code></strong>,连同 <code>plugin-ui.ts:10</code> 对 25 个 common 组件的 eager glob,把大半个工具集的逻辑代码静态编入入口</td></tr></tbody></table><p>此外存在一个<strong>双份 Monaco</strong>问题:除上述 ESM Monaco 外,<code>@tomjs/vite-plugin-monaco-editor</code>(<code>vite.config.ts:116</code>)还在 <code>&lt;head&gt;</code> 注入 3 个同步 AMD <code>&lt;script&gt;</code>(loader.js + editor.main.nls.js + editor.main.js)和 309 KB 渲染阻塞 CSS,供 @guolao 包装器使用;其中 <strong><code>editor.main.nls.js</code> 在产物中不存在,每次启动都是一次 404</strong>。</p><p>预期收益(详见分阶段建议):</p><ul><li>阶段一(低风险)后:启动载荷 17.5 MiB → 约 11–12 MB,<code>initMainApp</code> 1.2–1.5s → <strong>0.4–0.6s</strong>;</li><li>阶段二(结构治理)后:启动载荷 → <strong>约 3.5–4.5 MB(-75%)</strong>,空窗期随解析量等比缩短(WebView2 冷启收益最大);</li><li>dev 模式同样受益:eager glob 在 Vite dev 下意味着首屏数千个模块请求,去 eager 后 <code>tauri dev</code> 首屏将明显加快。</li></ul><hr><h2 id="一、启动链路全景" tabindex="-1">一、启动链路全景 <a class="header-anchor" href="#一、启动链路全景" aria-label="Permalink to &quot;一、启动链路全景&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[Rust] main.rs → lib.rs run()</span></span>
<span class="line"><span>  ├─ 同步读 settings.json / window-configs.json        (轻量,~ms)</span></span>
<span class="line"><span>  ├─ setup():</span></span>
<span class="line"><span>  │   ├─ AssetCatalog::initialize() 同步逐行读 JSONL    (lib.rs:821, 量随资产数增长)</span></span>
<span class="line"><span>  │   ├─ 创建主窗口 visible(false)                      (lib.rs:844)</span></span>
<span class="line"><span>  │   └─ 托盘 / 全局鼠标钩子</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>[WebView2] 加载 index.html</span></span>
<span class="line"><span>  ├─ &lt;head&gt; 同步 AMD Monaco: loader.js + nls(404!) + editor.main.js + 309KB CSS</span></span>
<span class="line"><span>  ├─ 内联 splash(主窗口不可见 → 用户看不到)</span></span>
<span class="line"><span>  └─ &lt;script type=&quot;module&quot; main.ts&gt; → 下载+解析 17.5MiB 静态图(145 文件)</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>[main.ts] 模块求值: 全量 EP/图标/SDK 挂 window → createApp → mount</span></span>
<span class="line"><span>  └─ mount 完成后 getCurrentWindow().show()             ← 窗口首次可见</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>[App.vue] LoadingScreen 显示, onMounted → initMainApp() ← 实测 1.22–1.48s, 纯串行:</span></span>
<span class="line"><span>  10% 设置加载 → 15% 日志 → 20% 模型元数据(~146ms) → 25% 主题(~1ms)</span></span>
<span class="line"><span>  → 30% Monaco/Shiki 23 种语言预热(135–190ms, await!)</span></span>
<span class="line"><span>  → 40% autoRegisterServices: 38 个 registry 串行 for-await(414–527ms)</span></span>
<span class="line"><span>  → 70% 用户档案(~55ms) → 80% 启动项任务(377–481ms, await!)</span></span>
<span class="line"><span>  → 90% 分离窗口管理(91–114ms) → 95% 同步总线(~1ms)</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>isReady = true → MainLayout 渲染(路由视图懒加载)</span></span></code></pre></div><p>两个关键的&quot;设计初衷 vs 实际效果&quot;错位:</p><ul><li><strong>防白屏策略</strong>(<code>0b5c9110</code>)让窗口等 mount 后再显示,初衷是消除位置/白屏闪烁;但入口膨胀后,它把全部解析成本转化成了&quot;无窗口期&quot;。</li><li><strong><code>autoRegisterServices</code> 的 <code>import.meta.glob</code>(非 eager,<code>auto-register.ts:47</code>)本是懒加载设计</strong>;但 <code>plugin-ui.ts:24</code> 对同一批文件做了 eager glob,使懒加载在产物层面完全失效——38 个 registry 及其静态依赖树照样进入口。</li></ul><h2 id="二、实测数据" tabindex="-1">二、实测数据 <a class="header-anchor" href="#二、实测数据" aria-label="Permalink to &quot;二、实测数据&quot;">​</a></h2><h3 id="_2-1-启动载荷-dist-实测-2026-06-11-构建" tabindex="-1">2.1 启动载荷(dist 实测,2026-06-11 构建) <a class="header-anchor" href="#_2-1-启动载荷-dist-实测-2026-06-11-构建" aria-label="Permalink to &quot;2.1 启动载荷(dist 实测,2026-06-11 构建)&quot;">​</a></h3><p><code>dist/index.html</code> 直接引用的资源合计 <strong>18,363,590 B(17.5 MiB)/ 145 个文件</strong>:1 个入口 script、128 个 <code>modulepreload</code>(即入口静态图的全部 chunk,启动时必然下载并执行)、15 个 CSS、外加 head 里的 AMD Monaco(loader.js 40KB + editor.main.js 79KB 存根 + editor.main.css 309KB;<code>npm/monaco-editor@0.55.1/</code> 目录另有 15 MB 按需 AMD 模块)。</p><p>Top 体积已在&quot;结论先行&quot;表格中列出。补充两个溯源细节:</p><ul><li><code>vendor-vue</code> 内含大量 TextMate scope 字符串(<code>punctuation.definition.tag.end.html</code>、<code>storage.modifier.vue-vine</code> 等)和 <code>vue-flow</code>、<code>sortable</code> 标记——证实 Shiki vue 系语法与 @vue-flow(llm-chat 树图专用)、vuedraggable 被 <code>includes(&quot;vue&quot;)</code> 规则误并;这些包本来只被<strong>懒加载</strong>代码引用,却因进入 vendor-vue 而变成启动必载。</li><li><code>model-metadata</code> chunk 内含 834 处 <code>&lt;svg</code>、1440 处 <code>lobe</code> 标记——即 <code>@lobehub/icons-static-svg</code> 全集以 raw 字符串形式内联(源头 <code>src/config/preset-icons.ts:13-16</code>),经 <code>modelMetadataStore</code>(启动链)与 <code>Avatar.vue</code>(common 组件,被 plugin-ui eager glob)两条路径进入入口。</li></ul><h3 id="_2-2-initmainapp-分步耗时-appdata-日志-2026-06-12-四次启动" tabindex="-1">2.2 <code>initMainApp</code> 分步耗时(AppData 日志,2026-06-12 四次启动) <a class="header-anchor" href="#_2-2-initmainapp-分步耗时-appdata-日志-2026-06-12-四次启动" aria-label="Permalink to &quot;2.2 \`initMainApp\` 分步耗时(AppData 日志,2026-06-12 四次启动)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>步骤(进度点)</th><th>run1 09:10</th><th>run2 10:31</th><th>run3 10:32</th><th>run4 10:39</th></tr></thead><tbody><tr><td>20%→25% 模型元数据规则</td><td>146ms</td><td>168ms</td><td>132ms</td><td>142ms</td></tr><tr><td>30%→40% Monaco/Shiki 预热</td><td>190ms</td><td>165ms</td><td>157ms</td><td>135ms</td></tr><tr><td>40%→60% 全量工具+插件注册</td><td>527ms</td><td>504ms</td><td>414ms</td><td>421ms</td></tr><tr><td>70%→80% 用户档案</td><td>55ms</td><td>58ms</td><td>44ms</td><td>~50ms</td></tr><tr><td>80%→90% 启动项任务</td><td>380ms</td><td>481ms</td><td>377ms</td><td>—</td></tr><tr><td>90%→95% 分离窗口管理</td><td>114ms</td><td>98ms</td><td>91ms</td><td>—</td></tr><tr><td><strong>全程(10%→100%)</strong></td><td><strong>1418ms</strong></td><td><strong>1479ms</strong></td><td><strong>1221ms</strong></td><td>~1.3s</td></tr></tbody></table><p>注:文件日志从 <code>applyLogConfig</code> 之后才开始写,mount 之前的解析耗时无法从日志取得,只能由载荷量推断(WebView2 有字节码缓存,冷启/热启差异大)。后续若需精确量化,建议在 <code>index.html</code> 内联脚本与 <code>main.ts</code> 头部打 <code>performance.mark</code>,并经 Tauri log 上报(见附录)。</p><h2 id="三、问题清单-按影响排序" tabindex="-1">三、问题清单(按影响排序) <a class="header-anchor" href="#三、问题清单-按影响排序" aria-label="Permalink to &quot;三、问题清单(按影响排序)&quot;">​</a></h2><h3 id="p0-1-plugin-ui-ts-双-eager-glob-整个工具集的逻辑代码静态进入口" tabindex="-1">P0-1 <code>plugin-ui.ts</code> 双 eager glob:整个工具集的逻辑代码静态进入口 <a class="header-anchor" href="#p0-1-plugin-ui-ts-双-eager-glob-整个工具集的逻辑代码静态进入口" aria-label="Permalink to &quot;P0-1 \`plugin-ui.ts\` 双 eager glob:整个工具集的逻辑代码静态进入口&quot;">​</a></h3><p><code>src/services/plugin-ui.ts:10-13</code>(25 个 common 组件)与 <code>:24-27</code>(38 个 <code>*.registry.ts</code>)都用 <code>{ eager: true }</code>。plugin-ui 被 <code>main.ts:10</code> 静态导入并挂到 <code>window.AiohubUI</code>(插件 ESM shim 契约)。</p><p>后果链:registry 文件普遍静态导入所在工具的 stores/composables/engine(如 <code>llm-chat.registry.ts</code> 拖入 services 与多个 composables;<code>token-calculator.registry.ts</code> 拖入 worker proxy 与 <code>@/config/model-metadata</code>;<code>smart-ocr.registry.ts</code> 经 <code>useImageSlicer</code> 链到 docxParser/jszip);common 组件里 <code>RichCodeEditor/DocumentViewer/TranscriptionDialog</code> 拖入 codemirror(vendor-editor 1.23MB)、docxParser(802KB)、转写引擎,<code>AudioPlayer</code>(301KB)、<code>PdfViewer</code> 等同理。<strong>保守估计该项贡献 5–7 MB 启动载荷</strong>,且使 <code>auto-register.ts</code> 的懒加载设计失效。</p><p>修复方向:两个 glob 去掉 <code>eager</code>,组件侧用 <code>defineAsyncComponent(async () =&gt; (await loader()).default / toolConfig.component())</code> 包装(组件名已从路径推导,<code>plugin-ui.ts:36-40</code>,不依赖模块内容);<code>window.AiohubUI</code> 契约不变——map 仍同步存在,值变为异步组件。插件首次渲染某组件时才加载对应 chunk。</p><h3 id="p0-2-stream-monaco-把-esm-monaco-shiki-静态链入启动-且预热被-await" tabindex="-1">P0-2 stream-monaco 把 ESM Monaco + Shiki 静态链入启动,且预热被 await <a class="header-anchor" href="#p0-2-stream-monaco-把-esm-monaco-shiki-静态链入启动-且预热被-await" aria-label="Permalink to &quot;P0-2 stream-monaco 把 ESM Monaco + Shiki 静态链入启动,且预热被 await&quot;">​</a></h3><ul><li>链路:<code>App.vue</code> → <code>appInitStore.ts:13</code> → <code>monacoShikiSetup.ts:14</code> 顶层 <code>import { registerMonacoThemes } from &quot;stream-monaco&quot;</code> → stream-monaco 顶层 <code>import * as monaco from &quot;monaco-editor&quot;</code> + <code>import { createHighlighter } from &quot;shiki&quot;</code> → <strong>editor.api2 3.63MB 进入口</strong>。</li><li>运行时:<code>appInitStore.ts:70</code> <code>await initMonacoShikiThemes()</code>,内部注册 <strong>23 种语言</strong>(<code>monacoShikiSetup.ts:28-52</code>)——加载 TM 语法 + oniguruma wasm,实测 135–190ms 全部计入首帧前。注释声称&quot;不阻塞应用启动&quot;(<code>monacoShikiSetup.ts:61</code>),实际语义只是&quot;失败不阻塞&quot;,成功路径是阻塞的。</li><li>叠加 @tomjs 插件的 AMD 注入,<strong>启动时存在两份 Monaco</strong>(head AMD 给 @guolao 用,ESM 给 stream-monaco 用)。</li></ul><p>修复方向:<code>monacoShikiSetup.ts</code> 改为函数体内 <code>await import(&quot;stream-monaco&quot;)</code>;<code>initMainApp</code> 不再 await 它(首帧后空闲触发,或由首个代码块渲染方 ensure——已有 <code>initPromise</code> 幂等保护,<code>isMonacoShikiReady()</code> 可作兜底)。中期统一到单份 ESM Monaco(见阶段二)。</p><blockquote><p><strong>2026-06-12 更新</strong>:本项已升级为整体移除方案——消息代码块默认引擎已是 CodeMirror 且实测性能更优,stream-monaco/shiki 将连同 <code>monacoShikiSetup.ts</code> 一并移除,详见 <a href="./stream-monaco-removal-plan">stream-monaco-removal-plan.md</a>。</p></blockquote><h3 id="p0-3-preset-icons-ts-内联-834-个-svg-2-34mb" tabindex="-1">P0-3 <code>preset-icons.ts</code> 内联 834 个 SVG(2.34MB) <a class="header-anchor" href="#p0-3-preset-icons-ts-内联-834-个-svg-2-34mb" aria-label="Permalink to &quot;P0-3 \`preset-icons.ts\` 内联 834 个 SVG(2.34MB)&quot;">​</a></h3><p><code>src/config/preset-icons.ts:13-16</code>:<code>import.meta.glob(&quot;@lobe-icons/*.svg&quot;, { eager: true, query: &quot;?raw&quot; })</code>。两条入口路径:<code>modelMetadataStore</code>(<code>appInitStore.ts:14</code>,启动链)与 <code>Avatar.vue</code>(common 组件,经 P0-1)。</p><p>修复方向(任选其一):</p><ul><li>改 <code>query: &quot;?url&quot;</code>(同文件 <code>localIcons</code> 已是此写法,<code>:20-26</code>):图标变成按需 fetch 的独立资源,map 仍同步可用(值为 URL),<code>LOBE_ICONS_MAP</code> 的消费方需从&quot;拿 raw 内容&quot;改为&quot;拿 URL&quot;(<code>Avatar</code>/<code>DynamicIcon</code> 渲染 <code>&lt;img&gt;</code> 或运行时 fetch);</li><li>或去 eager + 异步 getter,首次打开图标选择器/渲染头像时再加载。</li><li>注意 unplugin-icons 已配置 lobe 自定义集合(<code>vite.config.ts:130-139</code>),模板内 <code>i-lobe-*</code> 用法是按需编译的,与本问题无关,不要误伤。</li></ul><h3 id="p1-4-manualchunks-的-includes-vue-把懒资源变成启动必载" tabindex="-1">P1-4 <code>manualChunks</code> 的 <code>includes(&quot;vue&quot;)</code> 把懒资源变成启动必载 <a class="header-anchor" href="#p1-4-manualchunks-的-includes-vue-把懒资源变成启动必载" aria-label="Permalink to &quot;P1-4 \`manualChunks\` 的 \`includes(&quot;vue&quot;)\` 把懒资源变成启动必载&quot;">​</a></h3><p><code>vite.config.ts:194-213</code>:<code>if (id.includes(&quot;vue&quot;) || id.includes(&quot;vue-router&quot;) || id.includes(&quot;pinia&quot;)) return &quot;vendor-vue&quot;</code>。任何路径含 &quot;vue&quot; 的 node_modules 模块都被并入 vendor-vue——包括 Shiki 的 <code>vue/vue-html/vue-vine</code> 语法(本是 shiki 动态 import 的懒 chunk)、<code>@vue-flow/*</code>(仅树图视图用)、<code>@guolao/vue-monaco-editor</code>(本应进 vendor-editor)、sortablejs(vuedraggable 内联)。<strong>vendor-vue 因此膨胀到 3.95MB,且全部随入口加载</strong>。</p><p>修复方向:精确匹配,如 <code>/node_modules\\/(?:@vue|vue|vue-router|pinia|vue-demi)\\//</code>;或 Vite 8 下直接删掉手工 vue 分组,交给默认算法。修复后这些包回到各自的懒 chunk,首发收益即 2–3MB。</p><h3 id="p1-5-initmainapp-串行瀑布-且一半步骤与首帧无关" tabindex="-1">P1-5 <code>initMainApp</code> 串行瀑布,且一半步骤与首帧无关 <a class="header-anchor" href="#p1-5-initmainapp-串行瀑布-且一半步骤与首帧无关" aria-label="Permalink to &quot;P1-5 \`initMainApp\` 串行瀑布,且一半步骤与首帧无关&quot;">​</a></h3><p><code>src/stores/appInitStore.ts:41-110</code> 共 10 个串行 await。依赖分析:</p><ul><li>真正的首帧前置:<code>appSettingsStore.load()</code>(后续都读它)→ <code>initTheme()</code>、<code>autoRegisterServices()</code>(侧栏/路由需要 toolConfig)。</li><li>可并行:<code>modelMetadataStore.loadRules()</code>、<code>userProfileStore.loadProfiles()</code>(llm-chat 领域数据,首帧仅 HomePage/侧栏,完全可后置)、<code>initMonacoShikiThemes()</code>。</li><li>应后置到 <code>isReady</code> 之后:<code>startupManager.run()</code>(<code>appInitStore.ts:89</code>——用户自定义启动项,内部已有 10s 超时与熔断,<code>startup-manager.ts:113-121</code>,却阻塞首帧 377–481ms)、<code>detachedManager.initialize()</code>、<code>initializeSyncBus()</code>。</li></ul><p>修复方向:settings 先行,随后 <code>Promise.all([theme, autoRegister])</code> 达成即 <code>isReady = true</code>;其余在 <code>requestIdleCallback</code>/<code>setTimeout(0)</code> 中补齐。需验证两点:MainLayout 对 <code>useDetachedManager</code> 未初始化状态的容错(<code>MainLayout.vue:19</code>),以及分离窗口恢复流程对 syncBus 延迟初始化的容忍度(分离窗口路径本就有独立的 <code>initDetachedApp</code> 时序,<code>appInitStore.ts:115-178</code>)。</p><h3 id="p1-6-autoregisterservices-串行-for-await-38-个模块" tabindex="-1">P1-6 <code>autoRegisterServices</code> 串行 for-await 38 个模块 <a class="header-anchor" href="#p1-6-autoregisterservices-串行-for-await-38-个模块" aria-label="Permalink to &quot;P1-6 \`autoRegisterServices\` 串行 for-await 38 个模块&quot;">​</a></h3><p><code>src/services/auto-register.ts:165-172</code>:<code>for (const path of remainingPaths) { await loadAndRegisterModule(path, true) }</code>。每个模块一次动态 import(产物下对应 chunk 链)+ <code>register()</code>(内部 <code>initialize()</code> 多为空实现,个别如 tool-calling 等待 taskManager)。实测整段 414–527ms。</p><p>修复方向:<code>Promise.allSettled(remainingPaths.map(loadAndRegisterModule))</code>;<code>toolsStore.addTool</code> 的最终顺序由 <code>initializeOrder()</code> 统一决定(<code>auto-register.ts:185</code>),与注册先后无关,并行安全性较高;保留 failedModules 收集。P0-1 完成后,此项的网络/解析部分已大幅缩水,剩余为执行成本。</p><h3 id="p1-7-main-ts-全量-element-plus-全部-ep-图标挂-window" tabindex="-1">P1-7 <code>main.ts</code> 全量 Element Plus + 全部 EP 图标挂 window <a class="header-anchor" href="#p1-7-main-ts-全量-element-plus-全部-ep-图标挂-window" aria-label="Permalink to &quot;P1-7 \`main.ts\` 全量 Element Plus + 全部 EP 图标挂 window&quot;">​</a></h3><p><code>src/main.ts:7-12,40-44,94</code>:<code>import * as ElementPlus</code> + <code>import * as ElementPlusIconsVue</code>(全部图标)+ 全量 CSS(355KB)+ dark CSS,并挂到 <code>window</code> 供插件 ESM shim(<code>index.html</code> importmap → <code>public/plugins/shims/*</code>)。</p><p>修复方向(阶段二):这是插件契约,不能简单砍掉,但可以<strong>延后</strong>——插件系统本就在 <code>autoRegisterServices</code> 的 <code>loadRemaining</code> 尾部才初始化(<code>auto-register.ts:174-182</code>),<code>window.ElementPlus</code> 等全局只需在首个插件加载前就位。可改为:入口仅按需注册(unplugin resolvers 基建已在,<code>vite.config.ts:120-129</code>),<code>window.*</code> 全局改为在插件管理器初始化前 <code>await import(&quot;element-plus&quot;)</code> 赋值;或用 getter 惰性化。涉及插件兼容性,需配合 <code>docs/Plan/main-refactor-plugin-impact.md</code> 的契约清单评估。</p><h3 id="p2-8-tomjs-amd-monaco-同步-head-脚本、309kb-阻塞-css、nls-404、双份-monaco" tabindex="-1">P2-8 @tomjs AMD Monaco:同步 head 脚本、309KB 阻塞 CSS、nls 404、双份 Monaco <a class="header-anchor" href="#p2-8-tomjs-amd-monaco-同步-head-脚本、309kb-阻塞-css、nls-404、双份-monaco" aria-label="Permalink to &quot;P2-8 @tomjs AMD Monaco:同步 head 脚本、309KB 阻塞 CSS、nls 404、双份 Monaco&quot;">​</a></h3><p>产物 <code>dist/index.html:12-16</code>。<code>editor.main.nls.js</code> 不存在于 <code>dist/npm/</code>(monaco 0.55 已并入主文件),<strong>每次启动一次 404</strong>;loader/editor.main 为同步经典脚本,连内联 splash 的首绘都被其阻塞。该插件存在的唯一理由是给 <code>@guolao/vue-monaco-editor</code>(<code>RichCodeEditor.vue:51</code>)提供 <code>window.require</code> AMD 环境。</p><p>修复方向:短期把注入脚本改 <code>defer</code>/移除 nls 行(查 <code>@tomjs/vite-plugin-monaco-editor</code> 配置项);中期 <code>loader.config({ monaco })</code> 注入 ESM 实例(@guolao 官方支持),删除该插件,全 app 单份懒加载 ESM Monaco——同时让 <code>monaco-i18n</code> 的 ESM alias 劫持(<code>vite.config.ts:46-57</code>)成为唯一汉化路径。</p><h3 id="p2-9-窗口显示时机-mount-后才-show-内联-splash-形同虚设" tabindex="-1">P2-9 窗口显示时机:mount 后才 show,内联 splash 形同虚设 <a class="header-anchor" href="#p2-9-窗口显示时机-mount-后才-show-内联-splash-形同虚设" aria-label="Permalink to &quot;P2-9 窗口显示时机:mount 后才 show,内联 splash 形同虚设&quot;">​</a></h3><p><code>src/main.ts:183-191</code>。载荷缩减后此问题自动缓解;若想进一步消除空窗,可在 <code>DOMContentLoaded</code> 后即 <code>show()</code>(内联 splash 可见,且其样式已适配暗色),mount 后无缝切换 LoadingScreen——与防闪烁初衷不冲突,因为窗口尺寸/位置在 Rust 侧创建时已应用(<code>lib.rs:846-854,881-884</code>)。Linux 白屏 watchdog 依赖的 <code>frontend-ready</code> 事件不受影响(<code>main.ts:194-195</code>)。</p><h3 id="p2-10-rust-assetcatalog-initialize-同步阻塞在窗口创建前" tabindex="-1">P2-10 Rust:<code>AssetCatalog::initialize</code> 同步阻塞在窗口创建前 <a class="header-anchor" href="#p2-10-rust-assetcatalog-initialize-同步阻塞在窗口创建前" aria-label="Permalink to &quot;P2-10 Rust:\`AssetCatalog::initialize\` 同步阻塞在窗口创建前&quot;">​</a></h3><p><code>src-tauri/src/lib.rs:821-825</code> → <code>asset_manager.rs:41-83</code>,逐行 JSON 解析资产索引,资产多时(数千条)会推迟窗口创建。修复方向:<code>tauri::async_runtime::spawn</code> 后台初始化 + 首个资产命令处 ensure(已有 <code>RwLock</code> 结构,改动小)。同文件 <code>init_global_mouse_listener()</code>(<code>lib.rs:964</code>)为常驻钩子线程,开销一次性,暂不动。</p><h3 id="p2-11-杂项" tabindex="-1">P2-11 杂项 <a class="header-anchor" href="#p2-11-杂项" aria-label="Permalink to &quot;P2-11 杂项&quot;">​</a></h3><ul><li><code>main.ts:16-17</code> 全局导入 KaTeX CSS(含字体引用)与 viewerjs CSS:应随 rich-text-renderer / 图片查看器的 chunk 走。</li><li><code>App.vue:9</code> 静态导入 <code>MainLayout</code>:改 <code>defineAsyncComponent</code> 可让入口只含 LoadingScreen,进一步压薄 main chunk(收益中等,放阶段二顺手做)。</li><li><code>App.vue:49</code> 每 500ms 的 <code>setInterval</code> 滚动复位巡检:与启动无关但属常驻成本,可改为仅 scroll 事件驱动(已有 capture 监听)。</li><li><code>userProfileStore.loadProfiles</code> 全量加载(<code>cbed6864</code> 刚从按需改为全量):单次 ~55ms 不大,但它是 llm-chat 领域数据,不应在全局 <code>initMainApp</code> 里阻塞(归入 P1-5 的重排)。</li></ul><h2 id="四、分阶段建议" tabindex="-1">四、分阶段建议 <a class="header-anchor" href="#四、分阶段建议" aria-label="Permalink to &quot;四、分阶段建议&quot;">​</a></h2><h3 id="阶段一-低风险-单点改动-预计-1–2-天" tabindex="-1">阶段一:低风险,单点改动,预计 1–2 天 <a class="header-anchor" href="#阶段一-低风险-单点改动-预计-1–2-天" aria-label="Permalink to &quot;阶段一:低风险,单点改动,预计 1–2 天&quot;">​</a></h3><table tabindex="0"><thead><tr><th>#</th><th>改动</th><th>预期收益</th></tr></thead><tbody><tr><td>1</td><td><code>monacoShikiSetup</code> 动态 import + <code>initMainApp</code> 不 await(P0-2)</td><td>入口 -3.7MB;首帧 -150ms</td></tr><tr><td>2</td><td><code>preset-icons.ts</code> 改 <code>?url</code> / 去 eager(P0-3)</td><td>入口 -2.3MB</td></tr><tr><td>3</td><td><code>manualChunks</code> 精确匹配 vue 系(P1-4)</td><td>入口 -2~3MB(懒资源归位)</td></tr><tr><td>4</td><td><code>initMainApp</code> 重排:startup/detached/syncBus/profiles 后置,其余并行(P1-5)</td><td>首帧 -600~900ms</td></tr><tr><td>5</td><td><code>auto-register</code> 并行化(P1-6)</td><td>首帧 -200~350ms(与 4 部分重叠)</td></tr><tr><td>6</td><td>删 nls 404 行 / AMD 脚本 defer(P2-8 短期)</td><td>消除 404,splash 首绘提前</td></tr></tbody></table><p>阶段一完成后预估:载荷 ~11–12MB,<code>initMainApp</code> 0.4–0.6s。</p><h3 id="阶段二-结构治理-需要回归测试-预计-3–5-天" tabindex="-1">阶段二:结构治理,需要回归测试,预计 3–5 天 <a class="header-anchor" href="#阶段二-结构治理-需要回归测试-预计-3–5-天" aria-label="Permalink to &quot;阶段二:结构治理,需要回归测试,预计 3–5 天&quot;">​</a></h3><table tabindex="0"><thead><tr><th>#</th><th>改动</th><th>预期收益</th></tr></thead><tbody><tr><td>7</td><td><code>plugin-ui</code> 去 eager,异步组件包装(P0-1);同时收紧 <code>plugin-sdk.ts</code> 的 <code>export *</code> 面</td><td>入口再 -5~7MB,dev 首屏请求数大降</td></tr><tr><td>8</td><td>Element Plus 按需 + window 全局延后到插件系统初始化前(P1-7)</td><td>入口 -1~1.5MB</td></tr><tr><td>9</td><td>Monaco 统一 ESM,删 @tomjs AMD(P2-8 长期)</td><td>消除双份 Monaco;编辑器路径单一化</td></tr><tr><td>10</td><td><code>MainLayout</code> 异步化、KaTeX/viewer CSS 下沉(P2-11)</td><td>main chunk 进一步压薄</td></tr><tr><td>11</td><td>Rust <code>AssetCatalog</code> 后台化(P2-10)</td><td>窗口创建提前(资产多的用户)</td></tr></tbody></table><p>阶段二完成后预估:载荷 ~3.5–4.5MB(vue 生态 + EP 核心 + 入口逻辑)。</p><h3 id="阶段三-设计变更-按需立项" tabindex="-1">阶段三:设计变更,按需立项 <a class="header-anchor" href="#阶段三-设计变更-按需立项" aria-label="Permalink to &quot;阶段三:设计变更,按需立项&quot;">​</a></h3><ul><li><strong>registry 协议拆分</strong>:每个工具拆出轻量 manifest(id/名称/图标/path/runMode/detachableComponents 声明)与重实现;侧栏、路由、插件组件目录全部只依赖 manifest,实现首帧&quot;只读元数据、零工具逻辑&quot;,工具在首次进入/被调用时激活。这是 P0-1/P1-6 的根治形态,也与现有 <code>main-refactor-spec-v2.md</code> 的方向衔接。</li><li><strong>窗口提前显示</strong>(P2-9):载荷缩小后评估 <code>DOMContentLoaded</code> 即 show 的体验。</li><li><strong>启动 trace 常态化</strong>:<code>performance.mark</code> 打点 + 启动报告面板(现有 startupManager 已记录任务耗时,扩展到全链路),防止回归。</li></ul><h2 id="五、风险与兼容性注意" tabindex="-1">五、风险与兼容性注意 <a class="header-anchor" href="#五、风险与兼容性注意" aria-label="Permalink to &quot;五、风险与兼容性注意&quot;">​</a></h2><ol><li><strong>插件 ESM shim 契约</strong>:<code>window.Vue/ElementPlus/AiohubSDK/AiohubUI</code> 是插件运行时依赖(<code>index.html</code> importmap → <code>public/plugins/shims/</code>)。阶段二所有&quot;延后/按需&quot;改动必须保证:任何插件代码执行前这些全局已就位(插件加载本就在 <code>loadRemaining</code> 末尾,时序上可行,但要防插件管理器被第三方提前触发)。</li><li><strong>分离窗口路径共享同一入口</strong>:<code>main.ts</code> 根据 pathname 选根组件,P0-1/P0-2 的瘦身对分离窗口同样生效(它们目前也要解析同一份 17.5MB);<code>initDetachedApp</code> 的 priorityTool 机制不受影响。</li><li><strong><code>refreshCurrentRoute</code> 时序</strong>(<code>appInitStore.ts:78</code>):autoRegister 并行化后,确保路由刷新仍在全部 <code>addTool</code> 之后执行一次即可(现逻辑已满足——<code>loadRemaining</code> 内聚)。</li><li><strong>Shiki 主题欺骗机制</strong>(<code>monacoShikiSetup.ts:74-78</code>):延后初始化时,需保证任何 Monaco 实例 <code>setTheme(&quot;vs-dark&quot;)</code> 前主题已注册,或接受 stream-monaco 的增量注册兜底;<code>RichCodeEditor</code> 走 AMD 实例(阶段二统一前)不受影响。</li><li><strong>测量基线</strong>:本报告的 1.2–1.5s 为当前机器(开发者工作站、热缓存)数据;低端机/冷启的绝对值会更大,但瓶颈结构相同。</li></ol><h2 id="附录-复测方法" tabindex="-1">附录:复测方法 <a class="header-anchor" href="#附录-复测方法" aria-label="Permalink to &quot;附录:复测方法&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1. 量化启动载荷(构建后)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;…统计 dist/index.html 引用的 assets 总字节…&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 本次脚本:提取 href/src 求和,结果 18,363,590B/145 文件</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2. 真实启动分步耗时:启动应用后查看</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#    %APPDATA%/com.mty.aiohub/logs/app-YYYY-MM-DD*.log 中 [stores/appInitStore] 初始化进度 各行时间差</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 3. mount 前空窗期(待实施):index.html 内联 performance.mark(&#39;html-start&#39;),</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#    main.ts 头部 mark(&#39;module-eval&#39;),mountApp 内 mark(&#39;mounted&#39;),show() 后上报 measure</span></span></code></pre></div><p>主要证据文件:<code>src/main.ts</code>、<code>src/App.vue</code>、<code>src/stores/appInitStore.ts</code>、<code>src/services/plugin-ui.ts</code>、<code>src/services/auto-register.ts</code>、<code>src/utils/monacoShikiSetup.ts</code>、<code>src/config/preset-icons.ts</code>、<code>vite.config.ts</code>、<code>src-tauri/src/lib.rs</code>、<code>dist/index.html</code>(2026-06-11 构建)。</p>`,98)])])}const h=o(i,[["render",c]]);export{m as __pageData,h as default};
