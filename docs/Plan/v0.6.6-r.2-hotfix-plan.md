# v0.6.6-r.2 修复版筛选与实施清单

> 建立日期：2026-07-28
>
> 当前状态：首批独立修复已回移并通过自动化检查，等待真实 Tauri / 发布包验收
>
> 发布基线：`v0.6.6-r.1`（`4c1c11e6f`）
>
> 工作分支：`fix/0.6.6-r.2`
>
> 开发分支：`dev`（建立清单时指向 `f852e6b2d`）

## 1. 目标与边界

本分支用于制作桌面端 `0.6.6-r.2` 修复版。此前已经推送到 `origin/main` 但未进入发布标签的提交，不自动视为本修复版基线，也不要求全部进入修复版。

本轮只选择满足以下条件的改动：

1. 问题已经存在于 `v0.6.6-r.1`，或由该版本包含的数据/模块重构直接引入；
2. 对数据完整性、凭据安全、启动可用性、资源占用或高频主路径有明确影响；
3. 能在旧版本基线上独立验证，不依赖 0.7 的 Recall、Knowledge、Guided Flow、插件 API v3 等新架构；
4. 回移范围和风险可解释，可用自动化测试及真实 Tauri 运行证据支撑；
5. 每项修复独立提交，未经确认不合并到 `main`，不提前修改版本号或创建发布 tag。

## 2. 分支关系

```text
v0.6.6-r.1 (4c1c11e6f)
└── fix/0.6.6-r.2        # 本文所在分支，逐项回移并验收

origin/main (eba9d84b2)
└── main                 # 本地已恢复与远端一致，不作为修复版基线

f852e6b2d
└── dev                  # 保存原 main 上尚未同步的 0.7 开发历史
```

发布候选完成后，优先直接从 `fix/0.6.6-r.2` 构建并创建 `v0.6.6-r.2` tag。发布修复再按实际代码等价合入 `main` 和 `dev`，避免为了发版把未发布重构带入修复版。

## 3. 状态说明

| 状态   | 含义                                               |
| ------ | -------------------------------------------------- |
| 待调查 | 已知候选，尚未在发布基线上确认问题、依赖和回移方式 |
| 待实现 | 问题确认存在，但没有可直接使用的完整修复提交       |
| 待回移 | 已确认可从某个提交回移，尚未应用到本分支           |
| 实施中 | 正在本分支修改或解决冲突                           |
| 待验收 | 代码已完成，等待完整自动化或真实运行验收           |
| 已纳入 | 已通过验收并保留在修复版                           |
| 不纳入 | 确认不属于旧版本、依赖新架构或风险超过本轮收益     |
| 延后   | 问题成立，但本修复版不具备安全实施或充分验收条件   |

## 4. 已知待处理提交清单

提交标题中的 `fix`、`feat`、`refactor` 或 `chore` 不作为是否回移的唯一依据。必须检查问题是否存在于发布标签，以及提交是否混入新功能。

### 4.1 P0：数据、凭据和升级完整性

| 状态   | 来源提交                                | 候选修复                       | 当前判断与回移要求                                                                                                                                                   |
| ------ | --------------------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 待调查 | `f852e6b2d`                             | Agent 资产与搜索路径统一       | 问题由已发布的 Agent 解耦引入，属于修复版候选。来源提交基于 0.7 开发线并含后续计划文档，不应机械整提交 cherry-pick；需在标签基线上按文件和调用链移植并重新验证。     |
| 待调查 | `08cc18ea4`、`d2d02737f`                | LLM Chat 会话原子持久化与恢复  | 已有真实 NUL 污染、截断写、启动阻塞和删除后复活证据，严重度高。改动较大且与旧基线存在冲突，需作为独立工作包人工回移，不能只摘部分前端逻辑。                          |
| 待调查 | `08c80e3bb` 中进程锁 `.truncate(false)` | 会话持久化锁文件不被截断       | 仅在回移新会话持久化命令后适用；禁止整提交回移，因为该提交混有构建脚本重构及 0.7 才存在的渠道导出功能。                                                              |
| 待验收 | `cfc01247c` → `73f6a0235`               | Deep Link 凭据日志脱敏         | 改动文件相对发布标签无前置漂移，已整提交回移。URL、嵌套事件 payload、带引号 single-instance 参数测试通过；真实冷启动/热启动日志仍需在 Tauri 发布包中确认。           |
| 待验收 | `9d5c7cbc5` → `8fff6174b`               | 知识库删除时清理向量与内存状态 | 已整提交回移。前端失败不改 workspace、后端同时删除实体与向量目录、幂等删除测试通过；仍需真实 Tauri 验证内存索引/检索缓存和删除后立即查询。                           |
| 不纳入 | `88b77fbc8`                             | 全量知识库备份扫描持久化目录   | 标签基线没有可触发空 `kbIds` 的“导出全部”入口；来源提交新增的普通模式入口又被紧随其后的 `ec03e8b4d` 撤回，最终只留下无调用者的 `exportAll()`。不作为已发布缺陷回移。 |

### 4.2 P1：资源安全和高频主路径

| 状态   | 来源提交                  | 候选修复                             | 当前判断与回移要求                                                                                                                                     |
| ------ | ------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 待调查 | `343744fad`               | 目录搜索资源预算和完整取消生命周期   | 已确认宽范围搜索超时后 walker 继续运行并可能与新搜索重叠。来源提交跨 Rust、Tool Calling 和 VCP，需决定完整回移还是为修复版实现最小保护集。             |
| 待调查 | `b94d78ea7`、`5cb13afed`  | Key 健康策略统一及按渠道隔离熔断状态 | 问题可能造成一个渠道的 Key 状态影响其他渠道。两提交应作为一组评估，不能只回移一半；需同时验证桌面端设置、请求路径和旧配置兼容。                        |
| 待验收 | `2925ede91` → `6b3f4b4d0` | 活跃转写任务不被提前过滤             | 改动文件相对标签无前置漂移，已整提交回移。现有统一超时仍约束等待；类型检查和生产构建通过，尚需真实转写任务覆盖 pending/processing 到成功、失败和超时。 |
| 待验收 | `265f63795` → `98e84e7e5` | 模型路由不可用错误分类               | 发布标签已有相同 classifier 结构且改动文件无前置漂移，已整提交回移。`No available channel for model ...` 的 503 样本及 LLM Core 全部 probe 测试通过。  |
| 待调查 | `086d79733`               | 传输层 AbortSignal 传播              | 桌面流响应在响应头后中止时可能继续读取。来源提交同时修改移动端；桌面修复版应只评估桌面与共享合约部分，不默认带入移动端变更。                           |

### 4.3 首批直接回移验证记录（2026-07-28）

本批只处理来源提交改动文件相对 `v0.6.6-r.1` 无前置代码漂移、可以保持原提交边界的独立修复。未修改版本号。

- 前端 Vitest：`deepLinkRedaction.test.ts`、`kbStorage.test.ts`，共 2 个文件 5 项通过；
- LLM Core Vitest：`probe.test.ts`，18 项通过；
- Rust 测试：`cargo test --lib` 共 53 项通过，其中 `delete_base_directories` 2 项覆盖实体/向量目录与幂等删除；
- `bun run check:frontend`、`bun run build:vite`、LLM Core `tsc --noEmit` 通过；
- 定向 Oxlint、`cargo clippy -- -D warnings`、改动 Rust 文件的 `rustfmt --check`、`git diff --check` 通过；
- 全仓 `cargo fmt --check` 仍被发布标签已有的 `src-tauri/src/knowledge/commands/backup.rs` 单行格式差异阻断，本批没有修改该文件；
- Vite 仅输出既有的大 chunk、无效动态导入和第三方 `eval` 警告；
- 首次根 Vitest 启动因本地 `node_modules` 缺少锁文件已有的 `@tomjs/vite-plugin-monaco-editor` 失败；使用不修改清单/锁文件的本地安装补齐依赖后，测试与生产构建均通过。

### 4.4 明确不机械回移的开发线内容

| 状态   | 范围                                                    | 原因                                                         |
| ------ | ------------------------------------------------------- | ------------------------------------------------------------ |
| 不纳入 | `5d00b03cb` 及后续 Recall / Knowledge 领域拆分          | 属于未发布的大型重构，不是 `v0.6.6-r.1` 修复版基线。         |
| 不纳入 | 新 Recall SQLite 真源及其后续 `fix(recall)` 提交        | 修复的是 0.7 新架构，不适用于旧版本。                        |
| 不纳入 | Guided Flow、知识迁移引导及其后续修复                   | 0.7 新功能。                                                 |
| 不纳入 | Sidecar Plugin API v3、Monaco、Tokenizer 等启动治理改动 | 新功能或重构，不属于本轮稳定修复。                           |
| 不纳入 | `08c80e3bb` 中 LLM 渠道导出默认不含密钥                 | 渠道原生导出功能在发布标签后新增；旧版本没有该入口。         |
| 不纳入 | 移动资产管理器、Android content URI 和移动分页修复      | 移动端有独立版本、构建和 tag，本分支只制作桌面 `0.6.6-r.2`。 |
| 不纳入 | Native Plugin 开发模式 cwd / E2E 状态修复               | 主要影响开发与测试环境，尚无发布包用户影响证据。             |

## 5. 已知但尚无完整修复提交的问题

### 5.1 Agent 跨模块目录迁移不具备可靠恢复语义

位置：`src/tools/agent-manager/composables/storage/useAgentStorage.ts`

当前已知问题：

- 新目录只要存在任意实际条目就跳过迁移，部分复制或新旧目录并存时可能永久漏迁 Agent；
- `.migration_in_progress` 只被排除计数，没有恢复、继续或回滚状态机；
- 完整性验证只比较顶层条目数量，不比较名称、嵌套文件、文件大小、关键 JSON 可解析性或内容；
- 迁移失败后清理新目录但继续应用启动，用户可能只看到空 Agent 列表；
- 没有跨 WebView / 进程互斥，两个迁移调用可能互相复制、重命名或回滚；
- 备份目录没有 manifest、迁移 ID、校验结果和自动恢复入口。

最低验收场景：成功迁移、重复启动、目标目录部分存在、marker 残留、嵌套资产缺失、复制失败、校验失败、旧目录重命名失败、并发调用和旧目录只读恢复。

### 5.2 User Profile 首次升级存在竞态和永久跳过风险

位置：`src/tools/user-profile-manager/composables/storage/useUserProfileStorage.ts`、`src/tools/user-profile-manager/stores/userProfileStore.ts`

当前已知问题：

- 只有“新索引不存在且旧索引存在”才迁移，新索引一旦被创建为空文件就永久跳过旧数据；
- `loadProfiles()` 与 `loadSettings()` 并行，后者可能先由 `ConfigManager.load()` 创建默认新索引，从而使前者跳过迁移；
- 头像资产复制失败只记录 warning，之后仍提交新索引，下一次启动不再重试；
- 单个 Profile 加载失败后可能根据成功加载集合重写索引，进一步固化漏迁状态；
- 没有部分迁移状态、冲突处理、恢复入口和真实旧版本升级测试。

最低验收场景：并行首载、默认空新索引、部分目标目录、头像复制失败、索引写入失败、重复启动、`globalProfileId` 一致性和旧头像协议解析。

### 5.3 LLM Chat 会话持久化可能损坏用户数据

已知证据：

- `writeTextFile()` 截断正式文件后直接写入，进程退出可能留下空文件、部分内容或 NUL 填充；
- 流式保存周期短于单次持久化耗时，同一会话存在重叠写；
- 分离窗口可能参与加载、索引同步和修复，形成多写者；
- 删除与待写队列没有协调，迟到保存可复活已删除会话；
- 索引损坏会触发大量会话文件全量解析，造成明显启动阻塞。

本项若纳入，必须将前端单写者/合并队列、Rust 原子替换、索引备份、恢复语义和删除墓碑作为一个一致性边界实施，不能只换一个写文件 API。

### 5.4 目录搜索超时后可能继续消耗资源

已知问题：

- 超时不等于取消底层 walker；
- 新旧搜索可能重叠；
- 缺少统一深度、文件数、读取字节、deadline 和全局并发预算；
- Tool Calling 和 VCP 的 AbortSignal / cancel frame 没有完整传到节点执行；
- 替换类操作可能在截断或不完整搜索结果上继续写盘。

修复版至少需要保证本地搜索可取消、单活动搜索、有限 worker 和组合资源预算。是否回移完整 VCP 分布式取消协议需另行评估。

## 6. 建议实施顺序

1. 在发布标签上复现并冻结 Agent / User Profile 升级夹具；
2. 回移 Agent 活动路径修复，再实现两项迁移恢复，避免运行时和迁移同时指向不同目录；
3. 完成 Deep Link 脱敏与知识库删除的真实 Tauri 验收；全量备份候选已确认不属于标签基线可达功能；
4. 独立处理会话持久化，完成故障样本、退出中断、分离窗口和删除复活验证；
5. 决定目录搜索采用完整回移还是最小保护集；
6. 评估 Key 健康策略、转写、模型错误分类和传输取消；
7. 完成全量检查、真实 Tauri 升级测试和发布二进制 smoke test；
8. 使用 `bun run version:set -- desktop 0.6.6-r.2` 修改版本，运行 `bun run version:check`；
9. 人工确认变更清单后创建 `v0.6.6-r.2` tag 并发布；
10. 将已发布修复按等价变更同步到 `main` 和 `dev`，避免后续版本回归。

## 7. 每项候选的调查模板

每处理一项，在本文件对应条目下补充或链接调查记录，至少包含：

- 发布标签是否可复现；
- 用户影响和严重度；
- 来源提交及其前置依赖；
- 采用整提交 cherry-pick、拆分回移还是重新实现；
- 与 0.7 新架构的耦合点；
- 自动化测试；
- 真实 Tauri / 发布包验收；
- 是否纳入 `0.6.6-r.2` 的最终决定；
- 后续同步到 `main` / `dev` 的方式。

## 8. 当前门禁

在以下条件全部满足前，不修改版本号，不创建发布 tag：

- [ ] 已确认最终纳入范围；
- [ ] 所有纳入项均有独立提交和验证记录；
- [ ] Agent 与 User Profile 使用真实 `v0.6.6-r.1` 数据完成升级；
- [ ] 会话损坏和恢复若纳入，已通过故障样本与中断测试；
- [ ] 前端类型检查和 Vite 生产构建通过；
- [ ] Rust 格式、编译、Clippy 和测试通过；
- [ ] 发布二进制 smoke test 通过；
- [ ] `bun run version:check` 通过；
- [ ] 人工确认发布说明和剩余已知问题。
