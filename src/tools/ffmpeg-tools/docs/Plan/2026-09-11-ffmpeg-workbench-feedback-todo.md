# FFmpeg 工作台反馈改进待办

- 创建日期：2026-09-11
- 状态：待实施
- 反馈来源：kfc50，2026-09-11 16:01–17:27
- 范围：桌面端 `ffmpeg-tools` 与共享的 FFmpeg 后端执行入口
- 本轮仅记录研究结果和待办，不代表相关功能已经实现。

## 1. 目标与证据边界

优先保证任务可停止、输出文件安全、命令预览与实际执行一致，再补充文件项进度、参数积木编辑和时间裁剪。

以下问题依据当前源码审查记录，尚未在真实 Tauri 窗口逐项复现。涉及编码器能力、容器兼容性和终端转义的实现，需结合实际配置的 FFmpeg 构建与目标终端验证，不能仅凭命令字符串或浏览器 mock 判定完成。

### 原始反馈映射

| 反馈 | 对应待办 |
| --- | --- |
| 开始可连点、按钮不变成停止 | F01 任务生命周期与防重 |
| 添加视频后的自动命名不正确 | F02 自动命名与输出安全 |
| 文件项展示进度 | F05 文件反馈与进度 |
| Tag 积木式自定义参数 | F06 参数积木编辑 |
| 输出后缀跟随编码结果 | F03 输出容器与扩展名 |
| 路径带空格时复制指令无法在 PowerShell 使用 | F04 统一命令与终端复制 |
| `h264_nvenc` 使用 `-cq` 控制质量 | F07 编码器质量策略 |
| 专门的终端命令复制按钮 | F04 统一命令与终端复制 |
| 裁剪片段、关键帧吸附或精确重编码 | F08 时间裁剪 |

## 2. 当前实现与问题依据

下列路径均相对于仓库根目录。

| 区域 | 源码审查发现 | 主要文件 |
| --- | --- | --- |
| 提交 | 按钮仅检查输入文件是否存在；提交没有运行中锁，每次点击创建任务后立即启动 | `src/tools/ffmpeg-tools/components/FFmpegWorkbench.vue` |
| 取消 | 进程加入 `active_processes` 后又立即取出等待；取消入口从表中查找进程，找不到仍返回成功 | `src-tauri/src/commands/ffmpeg_processor.rs` |
| 终态 | 取消后，原执行 Promise 仍可能将状态写为完成或失败 | `src/tools/ffmpeg-tools/composables/useFFmpegCore.ts` |
| 命名 | 导入固定生成 `_processed` 并保留原扩展名；参数监听另有一套规则，且不监听输入文件和音频码率变化 | `src/tools/ffmpeg-tools/components/FFmpegWorkbench.vue` |
| 输出安全 | 重复提交可能使用相同输出路径；后端固定添加 `-y` | 工作台、后端执行入口 |
| 命令一致性 | 前端与 Rust 分别构造命令；禁用流、声道数、faststart、质量参数优先级等存在差异 | `src/tools/ffmpeg-tools/utils/command.ts`、后端执行入口 |
| 命令显示 | 输入路径未引用；输出只显示文件名；可执行程序固定显示 `ffmpeg` | `src/tools/ffmpeg-tools/components/FFmpegWorkbench.vue` |
| 自定义解析 | 字符串解析保留外层引用符后直接进入 argv；自定义参数统一放在输入 `-i` 后 | `src/tools/ffmpeg-tools/components/FFmpegCustomCommand.vue`、后端执行入口 |
| 编码质量 | 快捷质量字段统一使用 CRF，未按 NVENC 等编码器区分 | `src/tools/ffmpeg-tools/types.ts`、参数表单、命令构造 |
| 进度 | 后端已有 stderr 进度解析，任务监控已有进度展示；工作台文件卡片尚未接入 | 后端执行入口、`FFmpegTaskMonitor.vue`、工作台 |
| 监听与队列 | 工具入口和工作台重复注册监听；有并发数配置，但工作台启动链路没有调度限制 | `FFmpegTool.vue`、工作台、`ffmpegStore.ts` |
| 文件列表 | 工作台实际为单文件模式，多文件拖入仅取第一项 | `src/tools/ffmpeg-tools/components/FFmpegWorkbench.vue` |
| 裁剪 | 仅存在手写 `-ss / -to / -c copy` 的参数模板，没有区间与关键帧交互 | `src/tools/ffmpeg-tools/components/FFmpegCustomCommand.vue` |

## 3. 第一阶段：正确性与文件安全

### F01 — 任务生命周期与提交防重（P0）

状态：代码已实施（2026-09-11），纯逻辑与构建验证通过，真实 Tauri 运行态待走查。

- [x] 第一个异步操作前锁定提交，提交时固化本次参数与输入输出路径，避免后续编辑改变正在启动的任务。
- [x] 工作台按钮支持开始、启动中、停止、停止中；停止目标绑定明确的任务 ID。
- [x] 修复后端进程所有权与取消控制，覆盖元数据读取、启动、运行、退出各阶段的取消竞态。
- [x] 取消操作幂等；不得因进程查找不到就直接宣称已停止，须结合任务生命周期确认结果。
- [x] 统一终态写入，防止迟到的成功、错误或进度回调覆盖已确认的取消状态。
- [x] 活跃任务禁止占用同一个输出路径，路径预留与释放覆盖成功、失败和取消。
- [x] 复查 Agent `executeCommand`、`executePipeline` 的共享入口和取消信号，避免仅修复工作台。
- [x] 明确取消后的部分输出文件处理策略；不得误删用户原有文件，强制终止不承诺产物可播放。

验收：连点只启动一次；启动期间和处理中均可取消；真实子进程退出后界面才确认取消；再次运行不被旧回调污染；不同任务不能同时写同一路径。

实现要点：后端新增 `cancelled` 注册表与 `KillResult { found }`，取消错误统一为 `FFMPEG_CANCELLED`；进程在 `active_processes` 中保留所有权并轮询 `try_wait`，避免取消时找不到子进程。前端引入 `utils/lifecycle.ts`（终态判定、取消识别、输出路径预留纯逻辑），Store 忽略终态后的状态/进度回写并按任务维度释放路径预留，工作台按本地状态切换开始/启动中/停止/停止中。取消后不删除部分产物，也不得误删用户原文件。

验证：`bunx vitest run src/tools/ffmpeg-tools` 13 项通过；`bun run check:frontend` 通过；`bun run build:vite` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（cargo clippy 全仓库存在与本次无关的既有告警）。真实 Tauri 窗口的连点、启动/处理中取消与迟到回调未在本轮复现验证。

### F02 — 统一自动命名与输出安全（P0）

状态：代码已实施（2026-09-11），纯逻辑与构建验证通过，真实 Tauri 运行态待走查。

- [x] 合并导入、更换文件、参数变化与预设应用的命名规则，使用同一个纯逻辑入口。
- [x] 区分自动命名和用户自定义名称；手动改名后不静默覆盖，提供恢复自动命名。
- [x] 命名基于实际生效的参数，补齐输入文件、音频码率等依赖。
- [x] 处理无扩展名、多点文件名、特殊字符与名称长度边界。
- [x] 快速切换文件时忽略旧异步返回，避免名称、媒体信息和输入路径串联错位。
- [x] 提交前校验输入输出同路径、已有文件和活跃路径占用；不能只靠前端预览判断。
- [x] 自动名称冲突建议递增编号；用户明确指定已有文件时要求明确覆盖确认。最终策略在实施时落实，不保留无条件静默覆盖。

验收：先选音频/视频预设再导入、导入后切换模式、更换输入、修改音频码率均得到正确名称；手动名称保留；已有源文件不被误覆盖。

实现要点：新增 `utils/naming.ts` 纯逻辑入口（`splitFileName`、`buildParamsSuffix`、`resolveContainer`、`buildAutoOutputName`、`sanitizeOutputName`、`makeUniqueName`、`truncateFileName` 等），超长名称按 200 字符截断并保留扩展名。工作台移除本地 `paramsSuffix` 与旧输出名 `watch`，改用依赖输入文件、`metadata`（含源音视频编码）与全部命名相关参数的单一 `watch`；`outputNameCustomized` 区分自动/手动命名，手动改名后不再被参数变化覆盖并提供“恢复自动命名”。`handleFileDrop` 用 `fileLoadToken` 丢弃过期异步结果；`submitTask` 提交前用 `normalizeOutputPathKey` 校验输入输出同路径、`path_exists` 检查已存在文件：自动命名时递增编号，手动命名时弹窗确认覆盖。

### F03 — 输出容器与扩展名（P1）

状态：代码已实施（2026-09-11），纯逻辑与构建验证通过，真实 Tauri 运行态待走查。

- [x] 增加独立输出容器字段，支持自动选择和用户显式选择，不把视频编码器等同于文件格式。
- [x] 建立处理模式、音视频流和容器的最小兼容规则，扩展名跟随最终容器。
- [x] 音频流拷贝基于源音频编码推导候选容器，不能固定回退为 AAC/M4A。
- [x] 自定义 `-f` 与文件后缀冲突时提示；无法安全推导时按源容器/MP4 回退并展示提示，不静默猜测。
- [x] 修正 GIF 等模板的输出格式联动。
- [x] faststart 等容器相关选项仅在适用时生成。

验收：执行前即明确容器与路径；典型视频转码、音频提取、流拷贝和 GIF 输出的实际容器与扩展名一致；不兼容组合可解释地阻止或提示。

实现要点：`FFmpegParams` 新增 `container`，`MediaMetadata` 新增 `audioCodec`/`videoCodec`（Rust `get_video_metadata` 从 `ffmpeg -i` stderr 解析）。`resolveContainer` 按显式 container、自定义 `-f`、处理模式与源编码推导扩展名；`containerCompatibilityWarning` 对 MP4/MOV/M4A、WebM 及“纯音频容器承载视频流/GIF 承载音频流”等明显不兼容组合给出中文提示；`container` 纳入预设快照保存恢复。`executionPlan.buildOutputArgs` 的 faststart 判断在显式 container 下优先使用该容器。表单在非自定义模式显示输出容器选择（音频提取限定音频容器），切到自定义时清空 `container`。工作台在命令预览下展示容器/`-f` 后缀不一致与兼容性提示。

验证：`bunx vitest run src/tools/ffmpeg-tools` 52 项通过（新增 29 项命名/容器纯逻辑测试）；`bun run check:frontend` 通过；`bun run build:vite` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过、`ffmpeg_processor.rs` 无新增 clippy 告警。真实 Tauri 窗口的异步 `path_exists`、覆盖确认弹窗、实际输出容器与源编码解析未在本轮验证。

### F04 — 统一执行参数、预览与终端复制（P0）

状态：代码已实施（2026-09-11），纯逻辑与构建验证通过，真实终端与 Tauri 运行态待走查。

- [x] 建立唯一的最终命令构造入口；具体放在前端共享层还是后端，由实施时结合 Agent 调用选择，不继续维护两套独立规则。
- [x] 最终执行计划包含可执行文件、原始 argv、完整输入输出路径；为输入前和输出参数预留作用域。
- [x] 收敛禁用音视频、声道数、CRF/码率/目标体积优先级、像素格式、流拷贝及 faststart 等差异。
- [x] 修正自定义参数解析与序列化：去掉终端分组用的外层引号，但保留参数值本身必要的滤镜语法和转义。
- [x] 预览和复制使用同一份最终 argv，后端不再静默补入会改变处理结果的隐藏参数。
- [x] 新增复制 PowerShell 命令按钮，使用实际可执行文件与绝对输出路径，正确处理调用运算符和参数引用。
- [x] 复制器明确标注目标 Shell；不得把 PowerShell 字符串宣称为跨终端通用命令。其他 Shell 支持可后续扩展。
- [x] 区分应用内部进度/日志选项与外部运行版本，复制优化不得改变编码、裁剪或输出语义。
- [x] 更新快捷切换到自定义、预设保存恢复、Agent 命令与流水线的调用兼容性。

验收：预览和实际执行无语义偏差；空格、中文、单引号、美元符号和复杂滤镜不会因复制丢失参数边界；真实 PowerShell 可运行复制结果并产出预期媒体。

实现要点：新增 `utils/executionPlan.ts` 作为唯一构造入口，产出 `{ executable, globalArgs, inputArgs, inputPath, outputArgs, outputPath, args }`；`args = [globalArgs..., inputArgs..., "-i", inputPath, outputArgs..., outputPath]`。Rust 新增 `run_ffmpeg_plan` 只执行该 argv，不补充或重排任何参数（原 `process_media` 的参数构造已删除，转写引擎等调用方一并迁移）。`utils/args.ts` 重写解析/序列化：外层引号剥离、滤镜表达式保留为单参数，`quotePowerShellArg` 处理单引号转义。工作台预览与复制同用 `currentPlan`，复制按钮明确标注 PowerShell。

验证：`bunx vitest run src/tools/ffmpeg-tools` 23 项通过；`bun run check:frontend` 通过；`bun run build:vite` 通过；`cargo check` 通过、`ffmpeg_processor.rs` 无新增 clippy 告警。真实 PowerShell 终端执行复制结果、WebView 剪贴板与事件时机未在本轮验证。

### F07 — 编码器质量策略（P1，与 F04 同批）

状态：代码已实施（2026-09-11），软件路径逻辑验证通过，NVENC/QSV 缺硬件未验证。

- [x] 将质量模式与具体编码器选项解耦，区分目标质量、码率和目标体积。
- [x] NVENC 目标质量使用适配的 CQ/码率控制策略，不再直接套用 `-crf`；按实际 FFmpeg 编码器帮助验证可用选项。
- [x] 为当前暴露的编码器明确质量参数、范围及 preset 能力；不支持的组合不静默套用 x264 规则。
- [x] 切换编码器时清理冲突字段；流拷贝时不残留重编码滤镜或质量参数。
- [x] 更新类型、前后端传输、表单、快捷片段、预设快照与旧预设兼容。
- [x] 智能命名使用实际质量模式，例如 CQ，而不是一律标记 CRF。

验收：NVENC 不生成 CRF；切换编码器或质量模式不残留冲突参数；软件路径与有条件的硬件路径分别验证，缺少硬件不能记为已通过编码验证。

实现要点：`FFmpegParams` 新增 `qualityMode: "quality" | "bitrate" | "size"`；`resolveVideoQuality` 按编码器返回质量族（NVENC `-cq`、QSV `-global_quality`、VP9 `-crf ... -b:v 0`、其余 `-crf`）及范围；`resolveEncoderPreset` 将 x264 preset 映射为 NVENC `p1..p7`。ParamsForm 依据编码器切换 preset 选项并在 copy/none 时清理重编码字段；命名后缀使用实际质量标签（如 `cq25`）。NVENC/QSV 硬件路径缺少环境，未计入已通过编码验证。

## 4. 第二阶段：文件反馈与进度

### F05 — 文件项状态与结构化进度（P1）

状态：代码已实施（2026-09-11），纯逻辑与构建验证通过，真实 Tauri 运行态待走查；变速类目标时长分母随 F08 一并处理。

- [x] 先在当前单文件卡片展示关联任务的状态、百分比、速度、停止操作和失败原因。
- [x] 文件卡片与任务监控复用 Store 状态，不复制第二套进度源；更换文件不错误绑定上一任务。
- [x] 将全局事件监听收敛到一个明确生命周期，避免重复日志；覆盖离开工具后再返回的状态恢复。
- [x] 优先采用 `-progress pipe:1` 结构化进度，stderr 保留诊断日志；处理分块读取、未知值和流结束。
- [x] 未知总时长时展示不定进度；仅在进程成功退出后标记成功和 100%。
- [ ] 截取等已知变更使用目标输出时长作为分母；任意自定义变速命令无法推导时不展示伪精确百分比。（依赖 F08；未知时长已改为不定进度，变速命令仍未做目标时长识别）
- [x] 等待日志/进度读取收尾，避免终态后进度回退或遗漏关键错误。

验收：工作台与监控状态一致；日志不重复；未知时长、失败、取消均不会显示为成功；真正的文件写入与进程状态在 Tauri 运行态验证。

实现要点：后端 `run_ffmpeg_plan` 在 argv 输出路径前插入应用内部选项 `-progress pipe:1 -nostats`（不进入 `plan.args`，预览/复制不受影响），stdout 按行解析 `out_time/speed/bitrate/progress` 并对分块读取做行缓冲，`N/A`/未知值不覆盖已有值；stderr 仅输出日志。`FFmpegProgress` 增加 `totalDuration`，成功退出后才发 100%。`try_wait` 结束后以 2 秒超时 join 两个读取任务再返回，避免终态先于日志。工作台 `currentFileTask` 按归一化输入路径匹配任务，卡片展示状态/不定进度/速度/停止/失败原因；移除工作台重复的 `setupListeners`，由 `FFmpegTool.vue` 独占监听。

验证：`bunx vitest run src/tools/ffmpeg-tools` 53 项通过；`bun run check:frontend` 通过；`bun run build:vite` 通过；`cargo check` 通过、`ffmpeg_processor.rs` 无新增 clippy 告警。真实 Tauri 下 `-progress pipe:1` 解析、不定进度渲染与日志顺序未在本轮验证。

### F05-B — 真正的多文件队列（P2，独立扩展项）

- [ ] 将当前单文件草稿扩展成文件项模型，明确逐项参数快照、输出名与任务关联。
- [ ] 支持多文件导入、逐项状态和批量操作，不再只使用拖入路径的第一项。
- [ ] 接入并发数调度，明确工作台、Agent 与流水线共享资源的边界，避免突破限制。
- [ ] 排队取消不启动进程；新增、移除文件和重试不影响其他任务的输入输出绑定。

说明：文件卡片显示进度不等于已经实现批量处理；此项不阻塞单文件修复。

## 5. 第三阶段：编辑能力

### F06 — Tag 积木式自定义参数（P2，依赖 F04）

状态：代码已实施（2026-09-11），纯逻辑与构建验证通过，真实 WebView 交互待走查。

- [x] 在现有模板和快捷片段上演进，保留原始文本编辑入口。
- [x] 参数块以选项及其值为单位，支持无值开关、重复参数和显式顺序。
- [x] 支持编辑、删除、启用/禁用；排序须尊重参数作用域，不能任意跨输入输出边界拖动。
- [x] 区分全局、输入前和输出参数；滤镜表达式整体编辑。
- [x] 未识别参数保留为原始块，不强行配对或丢弃；解析错误明确提示。
- [x] 文本与积木双向转换保持 argv 语义，预设保存恢复保持顺序和重复项。

验收：复杂滤镜、负数参数值、重复 `-map`、无值开关及未知参数转换不损坏；不以 DOM 或标签样式断言代替语义验证。

实现要点：新增 `utils/paramBlocks.ts`（`parseArgsToBlocks`/`blocksToArgs`/`createParamBlock`/`hasRawBlocks`）作为纯逻辑；`FFmpegParams` 增加 `customGlobalArgs`/`customInputArgs`（原 `customArgs` 为输出作用域），`buildExecutionPlan` 自定义模式按 全局 → 输入前 → `-i` → 输出 顺序拼接。`FFmpegCustomCommand.vue` 在保留模板/快捷片段/原始文本编辑的基础上增加三作用域积木编辑（启用、编辑键值、上下移动仅限作用域内、删除、添加有值/无值块），未识别 token 保留为原始块并提示；文本与输出作用域积木双向同步，`presetParamKeys` 纳入两个新字段保证预设顺序与重复项。

验证：`bunx vitest run src/tools/ffmpeg-tools` 65 项通过（新增 11 项积木纯逻辑测试）；`bun run check:frontend` 通过；`bun run build:vite` 通过；`bunx oxlint src/tools/ffmpeg-tools` 无告警。真实 WebView 中的作用域切换、拖动排序、原始块提示与双向同步交互未在本轮验证。

### F08 — 时间片段裁剪（P2，依赖 F03/F04/F05/F07）

- [ ] 提供起止时间输入、从播放位置设置边界和单区间选择；此处是时间裁剪，不是画面区域裁剪。
- [ ] 快速模式使用流拷贝，提供关键帧吸附策略并展示实际采用的起点；不承诺任意时刻精确无损截取。
- [ ] 精确模式明确提示重编码，使用所选编码质量策略。
- [ ] 按需通过 ffprobe 获取关键帧信息，考虑长视频开销、缓存与过期请求取消。
- [ ] 明确吸附方向、输出时间戳和音视频边界策略，展示请求区间与实际区间的差异；不能将关键帧等同于所有容器的绝对安全边界。
- [ ] 校验起止顺序、超出源时长、零时长和音频文件等情况。
- [ ] 统一预览、复制、命名、进度与实际裁剪参数。

验收：含关键帧间隔的视频、可变帧率视频和带音频素材验证可播放性、起止边界与同步；精确模式按帧/时间基允许误差验收，不承诺任意毫秒均存在对应帧。

首版不包含多段拼接、边界局部重编码或完整时间线编辑器。

## 6. 验证与文档同步

### 验证原则

- [ ] 实施前重新读取相关源码与 `package.json` 脚本，测试只覆盖本次改动风险。
- [ ] 纯逻辑回归重点：防止命名覆盖、参数边界丢失、质量模式冲突、终态反转和重复输出路径占用；新增测试先说明防止的具体回归。
- [ ] 后端验证真实进程取消、启动失败、自然退出与取消竞态，不以 mock invoke 成功代替。
- [ ] 前端改动执行已有类型检查与 Vite 生产构建脚本。
- [ ] 在真实 Tauri 窗口检查文件导入、启动/停止、状态恢复和布局交互；普通浏览器仅用于明确支持的纯前端场景。
- [ ] 使用少量确定性媒体素材检查实际输出流、容器、时长和裁剪边界；NVENC 无环境时记录未验证。
- [ ] PowerShell 复制功能在真实终端验证，不只检查字符串包含引号。

### 文档同步

- [x] 实现后更新本工具 `ARCHITECTURE.md` 的真实行为和数据流，并回填本文待办及验证结果。
- [x] 架构文档现有“支持多文件批量导入”“并发控制”“可精确终止”等表述与当前代码存在偏差，实施时按实际能力修订，不作为已经实现的依据。（已修订“多文件批量导入”为单文件模式；其余表述待对应功能实施时再核对）
- [ ] 当前进度时长由后端自行读取元数据，架构文档中的“前端传入 duration”描述需同步修正。
- [ ] 重大方案变化或阻塞写回本文；未完成验证的条目不能仅凭代码已提交/已编译勾选完成。

## 7. 推荐实施顺序

1. F01：生命周期、防重与输出路径占用。
2. F04 + F07：唯一执行计划、参数正确性、终端复制与编码器质量策略。
3. F02 + F03：自动命名、容器与文件覆盖策略，与执行计划联动。
4. F05：单文件卡片反馈与进度；F05-B 按批量需求单独实施。
5. F06 / F08：依赖底层能力稳定后增加参数积木与时间裁剪。

第一阶段各项存在交叉，按完整行为闭环交付，不为了拆批次保留预览与执行不一致。
