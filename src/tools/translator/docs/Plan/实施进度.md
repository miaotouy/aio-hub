# 翻译工作台 v2 · 实施进度

> **状态**: Implementing
> **关联文档**: [`docs/design/translation-workbench-plan-v2.md`](../../../../../docs/design/translation-workbench-plan-v2.md)
> **更新时间**: 2026-05-31

## 1. 当前快照

| 阶段                              | 状态        | 备注                                  |
| --------------------------------- | ----------- | ------------------------------------- |
| Phase 1 · MVP                     | ✅ 完成     | 多渠道并行、流式、并排展示均可用      |
| Phase 2 · 预设系统 + 配置持久化   | 🟡 部分完成 | 持久化已落地，预设 CRUD UI 缺         |
| Phase 3 · 体验打磨 + 历史记录     | 🟡 部分完成 | 历史持久化 / 自动吸底 OK，Diff/搜索缺 |
| Phase 4 · 高级功能（批量/评分等） | ❌ 未开始   | 可选项                                |

## 2. 已完成功能清单

### 2.1 核心翻译能力

- 多渠道并发翻译，每个渠道独立 `AbortController`，互不阻塞
- 渠道粒度的流式输出（设置可关）
- 渠道粒度的"停止 / 重试"按钮
- 流式累积内容与最终响应的"最长保护"，规避不同 LLM 适配器返回不一致
- 状态机：`idle / pending / streaming / completed / aborted / failed`
- `finish_reason === 'max_tokens' | 'length'` 自动显示"输出截断"警告徽章

### 2.2 输出长度自适应

- 设置项：默认 maxTokens、按输入长度自动扩容、输出膨胀因子、默认温度
- 估算公式：`charCount × 膨胀因子 + 段落格式预留(clamp 512~4096)`
- 与模型自身 `tokenLimits.output` 做 `min()` 并 clamp 到 `[256, 131072]`
- 卡片底部展示「实际 max / 模型上限」对照

### 2.3 持久化（已迁到 ConfigManager）

落盘位置：`appDataDir/translator/`

| 文件            | 内容                                                          | 防抖  |
| --------------- | ------------------------------------------------------------- | ----- |
| `settings.json` | 全局参数（maxTokens / 温度 / 流式 / 历史开关）                | 400ms |
| `history.json`  | 最近 30 条翻译历史                                            | 600ms |
| `presets.json`  | 预设数组（含每个渠道的 profileId / modelId）+ 当前激活预设 ID | 400ms |

实现细节：

- `initialize()` 并发加载三份 + LLM Profiles
- `isLoadingPersistence` 哨兵防"加载即回写"循环
- 启动时校验 `activePresetId` 是否还存在，不存在降级到首个预设
- `clearHistory()` 立即落盘空数据，不依赖防抖

### 2.4 UI / 交互

- 预设 Tab 切换栏，最多展示当前预设的渠道数 badge
- 输入面板：源/目标语言下拉 + 一键互换、Ctrl+Enter 发送（符合项目规范）
- 渠道面板：增加/删除/换模型（最多 6 个，最少 1 个）
- 结果面板：响应式 grid 并排卡片，流式自动吸底（用户手动上滑暂停，回到底部恢复）
- 设置对话框：使用 [`BaseDialog`](../../../../components/common/BaseDialog.vue) 并遵循 `lockScroll: false` 规范
- 历史 strip：最近 8 条快速回填

### 2.5 注册与排序

- [`translator.registry.ts`](../../translator.registry.ts) 同时导出 `toolConfig` + `TranslatorRegistry` 类
- 已添加到 [`src/config/tools.ts`](../../../../config/tools.ts) `DEFAULT_TOOLS_ORDER` 的"文本处理"组首位
- 图标 `markRaw(Languages)`，符合 `.kilocode/rules/rules.md` 硬性要求

## 3. 待办清单（按优先级）

### 🔴 高优先级 / Phase 2 收尾

1. **预设 CRUD 对话框**
   - 新建预设：名称、图标（从 Lucide 选）、Prompt 模板、默认源/目标语言、初始渠道集合
   - 编辑预设：在不影响 channelId 的前提下改名/换图标/改 Prompt
   - 删除预设：保留至少 1 个，删除当前激活预设时自动跳转
   - 排序：拖拽或上下移动按钮
   - **挂载点**：预设栏末尾的「⚙️ 管理」按钮，弹出独立对话框（**不要**复用现有 `TranslatorSettingsDialog`）

2. **预设级语言粘性策略决策**
   - 当前问题：[`setActivePreset()`](../../composables/useTranslatorStore.ts) 切换 Tab 时会强制重置 `sourceLang/targetLang` 为预设的 `defaultSourceLang/defaultTargetLang`，覆盖用户手选
   - 候选方案：
     - A. 维持现状（预设即"场景"，强语义绑定）
     - B. 切换时仅当 `sourceLang === preset.previousSource && targetLang === preset.previousTarget` 才重置，否则保留用户选择
     - C. 加一个全局设置项「切换预设时保留语言选择」
   - **建议先与姐姐讨论后实现**

### 🟡 中优先级 / Phase 3 收尾

3. **历史搜索 + 完整历史抽屉**
   - 底部 strip 升级为可展开抽屉，支持文本搜索 / 按预设过滤 / 按日期分组
   - 点击历史条目可"重新翻译"或仅"加载文本"
4. **结果 Diff 高亮**
   - 与 [`text-diff`](../../../text-diff) 共享算法？或自研字符级 diff
   - 仅在"完成"状态展示，流式过程中不计算
5. **语言列表自定义**
   - 当前硬编码 9 种语言在 [`constants.ts`](../../constants.ts)
   - 改成可在设置面板增删，落 `settings.json`

### 🟢 低优先级 / 体验加分

6. **DropZone 拖入文本文件** — 复用 [`useFileDrop`](../../../../composables/useFileDrop.ts) / [`useFileInteraction`](../../../../composables/useFileInteraction.ts)
7. **导出对比报告** — 多渠道结果 + 耗时 + token 统计导出 Markdown / JSON
8. **与 llm-chat 联动** — 聊天中选中文本 → 跳转翻译工作台（需在 llm-chat 侧加 contextmenu）

### ❌ 已确认不做（与计划保持一致）

- 传统翻译 API（DeepL / Google / Microsoft）
- 图片/文档翻译（归 smart-ocr + VLM）
- 翻译记忆库 / 术语表管理
- 全局翻译抽象层

## 4. 与原计划的演进差异

| 计划文档                                                 | 实际实现                                                                         | 处理                                  |
| -------------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------- |
| 单个 `appConfigDir/translator/presets.json`              | 拆成 `settings.json / history.json / presets.json` 三文件                        | 三文件职责更清晰，已采用              |
| `useTranslatorCore.translateParallel()` 在 core 层做并发 | 并发逻辑下沉到 Store 的 [`translate()`](../../composables/useTranslatorStore.ts) | Store 需掌控状态机/控制器，下沉更合理 |
| `TranslationResult` 字段仅 `isStreaming + error`         | 增加 `status / startedAt / appliedMaxTokens / modelOutputLimit / finishReason`   | 更细的状态机便于 UI 反馈              |
| 计划未涉及 maxTokens 自动估算                            | 完整的 `estimateTranslationOutputTokens()` + clamp 链                            | 解决长文翻译被截断问题                |
| 计划只有 `abortAll`                                      | 增加 `abortChannel(id) / retryChannel(id)`                                       | 单渠道失败可独立救                    |

## 5. 关键文件索引

- 入口组件：[`Translator.vue`](../../Translator.vue)
- 注册：[`translator.registry.ts`](../../translator.registry.ts)
- 类型：[`types.ts`](../../types.ts)
- 常量（语言列表）：[`constants.ts`](../../constants.ts)
- 核心翻译 composable：[`useTranslatorCore.ts`](../../composables/useTranslatorCore.ts)
- 状态 Store：[`useTranslatorStore.ts`](../../composables/useTranslatorStore.ts)
- 子组件：
  - [`InputPanel.vue`](../../components/InputPanel.vue)
  - [`ResultsPanel.vue`](../../components/ResultsPanel.vue)
  - [`TranslatorSettingsDialog.vue`](../../components/TranslatorSettingsDialog.vue)

## 6. 验证记录

- `bun run check:frontend` → 通过（vue-tsc --noEmit 退出码 0）
- `bun run check:backend` → 未涉及 Rust 改动，无需验证
- 手动验证场景（**待办**）：
  - [ ] 首次启动生成默认预设
  - [ ] 改动渠道后重启保留
  - [ ] 清空历史后重启仍为空
  - [ ] 删除 LLM Profile 后，预设里引用该 Profile 的渠道表现（**已知未处理，需补降级 UI**）
